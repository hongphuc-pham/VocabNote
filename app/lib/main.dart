import 'package:vocabnote/bootstrap.dart';

/// Entry point.
///
/// Deliberately empty: everything that could fail or take time lives in
/// [bootstrap], where it can be reasoned about and tested.
void main() => bootstrap();
